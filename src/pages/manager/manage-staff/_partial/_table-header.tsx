export default function TableHeader() {
  return (
    <thead className="bg-blue-600 text-sm">
      <tr>
        <th className="!py-2 !text-center !text-white">No.</th>
        <th className="!py-2 !text-center !text-white">Tên nhân sự</th>
        <th className="!py-2 !text-center !text-white">Vai trò</th>
        <th className="!py-2 !text-center !text-white">SĐT</th>
        <th className="!py-2 !text-center !text-white">Hành động</th>
      </tr>
    </thead>
  );
}
